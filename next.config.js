/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable if you need image optimization
  images: {
    remotePatterns: [
      // Add domains for your CMS images later
    ],
  },
}

export default nextConfig